import React from 'react';
import { BrowserRouter as Router, Switch, Route } from 'react-router-dom';

//Components

function App() {
	return (
		<Router>
			{/* Rota para responder os forms por link (incompleta, falta a parte da pessoa que está montando os forms) */}
		</Router>
	);
}

export default App;
